# 实时温柔提醒功能 - 完整设计方案

> **版本：** v1.0  
> **日期：** 2026年2月16日  
> **核心理念：** 朋友式陪伴，不教育、不打扰、引导觉察

---

## 📋 目录

1. [功能概述](#功能概述)
2. [触发逻辑设计](#触发逻辑设计)
3. [文案库设计](#文案库设计)
4. [交互设计](#交互设计)
5. [技术实现](#技术实现)
6. [代码示例](#代码示例)
7. [A/B测试方案](#ab测试方案)

---

## 一、功能概述

### 1.1 核心目标

**不是打断用户，而是唤醒用户的自我觉察**

```
用户刷视频 → 时长统计 → 达到阈值 → 温柔提醒 → 引导思考 → 自主决策
```

### 1.2 设计原则

| 原则 | 说明 | 反例 |
|-----|------|-----|
| **温柔** | 像朋友关心，不像家长教育 | ❌ "你已经刷太久了！" |
| **疑问** | 用问题引导，不用命令强制 | ❌ "马上停下来休息！" |
| **具体** | 给出具体行动，不给空洞建议 | ❌ "去做点有意义的事吧" |
| **尊重** | 可忽略可操作，完全自主 | ❌ 强制关闭APP |
| **简洁** | 一句话说清楚，不啰嗦 | ❌ 弹窗显示3段文字 |

### 1.3 用户价值

1. **觉察时间流逝**（"原来已经刷了1小时了"）
2. **唤醒初心**（"我本来要查资料的"）
3. **引导思考**（"我为什么在刷？"）
4. **提供出口**（"起来倒杯水吧"）

---

## 二、触发逻辑设计

### 2.1 时长阈值策略

采用**渐进式提醒**，从轻到重：

```
时长阶段           提醒强度        文案风格              频率
─────────────────────────────────────────────────────
15分钟            轻提醒         疑问+关心            仅1次
30分钟            中提醒         疑问+唤醒初心         仅1次
1小时             强提醒         疑问+行动引导         仅1次
2小时             重提醒         疑问+关怀+具体行动     仅1次
4小时             极重提醒       直接关怀+强行动引导    每30分钟1次
```

**关键设计：**
- 每个阶段只提醒1次（避免打扰）
- 4小时后才开始重复提醒（用户可能真的需要干预）
- 用户可手动关闭提醒（尊重选择）

---

### 2.2 触发时机优化

**不是刚好到阈值就弹窗，而是找合适的时机**

```javascript
触发条件 = 达到时长阈值 + 满足以下任一条件：
├─ 用户暂停视频
├─ 用户切换视频
├─ 用户滑动到下一个视频
└─ 用户停止操作超过3秒（可能在发呆）
```

**反例：用户正在专注看视频时弹窗 → 极度打扰**

---

### 2.3 场景识别逻辑

根据用户记录的"动机"，调整提醒文案：

| 用户动机 | 提醒策略 | 文案示例 |
|---------|---------|---------|
| **学习** | 温柔，不打断 | "学到什么了吗？要不要记下来？" |
| **放松** | 关怀，提醒休息 | "放松一下挺好的，累不累？" |
| **逃避** | 引导觉察 | "在逃避什么呢？想聊聊吗？" |
| **无聊** | 提供替代行动 | "无聊的话，要不要出去走走？" |
| **未记录** | 引导记录动机 | "你刚刚想做什么来着？" |

---

### 2.4 智能静默机制

**某些情况下，不提醒：**

```
不提醒的场景：
├─ 用户刚刚回应了上一次提醒（5分钟内）
├─ 用户手动关闭了提醒（当天不再提醒）
├─ 用户正在记录（正在使用APP）
├─ 深夜时段（23:00-07:00，避免打扰睡眠）
└─ 用户设置了"专注模式"（例如正在学习）
```

---

## 三、文案库设计

### 3.1 文案结构

**标准格式：时长 + 疑问 + 引导**

```
[时长陈述] + [温柔疑问] + [具体引导]
    ↓           ↓           ↓
 "刷了1小时了" + "累不累？" + "起来走走？"
```

---

### 3.2 完整文案库

#### **阶段1：15分钟（轻提醒）**

**目的：唤醒初心**

```
1. "刷了15分钟了，你刚刚想做什么来着？"
2. "15分钟过去了，找到想看的了吗？"
3. "刷了一会儿了，还记得原本要做什么吗？"
4. "15分钟了，要不要先做正事？"
5. "刷了15分钟，你是来学习的吗？还是放松一下？"
```

**交互选项：**
```
[还记得] [忘了] [再看一会儿]
```

---

#### **阶段2：30分钟（中提醒）**

**目的：引导觉察 + 提供出口**

```
1. "刷了半小时了，学到什么了吗？"
2. "30分钟过去了，记得什么内容吗？"
3. "半小时了，累不累？要不要休息一下？"
4. "刷了30分钟，你在找什么呢？"
5. "半小时了，是在放松吗？感觉怎么样？"
6. "30分钟过去了，眼睛累不累？"
```

**交互选项：**
```
[学到了] [没什么印象] [起来休息] [再看一会儿]
```

---

#### **阶段3：1小时（强提醒）**

**目的：关怀 + 具体行动引导**

```
1. "刷了1小时了，起来倒杯水吧？"
2. "1小时过去了，站起来走走？看看窗外？"
3. "刷了1小时，眼睛休息一下吧？"
4. "1小时了，伸个懒腰？动一动？"
5. "刷了1小时了，要不要先去做那件事？"
6. "1小时过去了，你在逃避什么吗？"
7. "刷了1小时，今天还有其他想做的事吗？"
```

**交互选项：**
```
[去倒水] [走走] [继续看] [记录一下]
```

**关键设计：**
- 给出**具体行动**（倒水、走走、看窗外）
- 不是空洞的"休息一下"

---

#### **阶段4：2小时（重提醒）**

**目的：深度关怀 + 引导觉察**

```
1. "刷了2小时了，累不累？认真问的。"
2. "2小时过去了，今天记得什么了吗？"
3. "刷了2小时，是不是有什么心事？"
4. "2小时了，要不要出去透透气？"
5. "刷了2小时，眼睛该休息了吧？"
6. "2小时过去了，你在逃避什么呢？想聊聊吗？"
7. "刷了2小时，今天还有什么想做的事吗？"
```

**交互选项：**
```
[确实累了] [出去走走] [想聊聊] [记录心情] [继续看]
```

**关键设计：**
- 增加"认真问的"（表达真诚关心）
- 增加"想聊聊吗"（提供情感支持）

---

#### **阶段5：4小时（极重提醒）**

**目的：强关怀 + 强引导 + 提供帮助**

```
1. "刷了4小时了，真的累了吧？起来动一动？"
2. "4小时过去了，眼睛、脖子、腰都该休息了"
3. "刷了4小时，今天过得怎么样？想聊聊吗？"
4. "4小时了，是不是有什么不想面对的事？"
5. "刷了4小时，要不要记录一下今天的心情？"
6. "4小时过去了，出去走走吧，认真的"
7. "刷了4小时，今天还好吗？需要帮助吗？"
```

**交互选项：**
```
[记录心情] [出去走走] [想聊聊] [设置提醒] [继续看]
```

**关键设计：**
- 从疑问变陈述（"真的累了吧"）
- 增加情感关怀（"今天还好吗？"）
- 提供记录心情的出口

---

#### **阶段6：6小时+（持续提醒）**

**目的：严肃关怀 + 引导寻求帮助**

```
1. "刷了6小时了，今天还好吗？真的。"
2. "6小时过去了，是不是遇到什么事了？"
3. "刷了6小时，要不要找个朋友聊聊？"
4. "6小时了，需要帮你联系谁吗？"
5. "刷了6小时，记录一下今天发生了什么吧"
```

**交互选项：**
```
[记录今天] [找朋友聊] [寻求帮助] [我没事]
```

**关键设计：**
- 6小时是严重信号，可能需要外部帮助
- 提供"寻求帮助"的出口

---

### 3.3 动机感知文案（结合用户记录的动机）

#### **如果用户记录了"学习"动机：**

```
15分钟：
- "学了15分钟了，要不要记个笔记？"
- "15分钟了，学到什么了？"

30分钟：
- "学了半小时，记得什么了吗？"
- "30分钟了，要不要整理一下笔记？"

1小时：
- "学了1小时了，休息一下，劳逸结合？"
- "1小时过去了，今天的学习目标完成了吗？"
```

---

#### **如果用户记录了"放松"动机：**

```
15分钟：
- "放松15分钟了，感觉好点了吗？"
- "15分钟了，心情好点了吗？"

30分钟：
- "放松半小时了，够了吗？"
- "30分钟了，感觉轻松些了吗？"

1小时：
- "放松1小时了，要不要换个方式？出去走走？"
- "1小时了，放松得差不多了吧？"
```

---

#### **如果用户记录了"逃避"动机：**

```
15分钟：
- "在逃避什么呢？要不要先面对一下？"
- "15分钟了，逃避能解决问题吗？"

30分钟：
- "逃避半小时了，那件事还在等你呢"
- "30分钟了，要不要试着做一下？"

1小时：
- "逃避1小时了，其实可能没那么难？"
- "1小时了，要不要先做5分钟试试？"
```

**关键设计：**
- 对"逃避"更直接一些（因为用户已经承认了）
- 提供具体行动（"先做5分钟试试"）

---

#### **如果用户记录了"无聊"动机：**

```
15分钟：
- "无聊了15分钟，要不要换个方式？"
- "15分钟了，还无聊吗？"

30分钟：
- "无聊半小时了，要不要出去走走？"
- "30分钟了，有没有想做的事？"

1小时：
- "无聊1小时了，要不要找点有意思的事做？"
- "1小时了，真的没有想做的事吗？"
```

---

### 3.4 时段感知文案（结合当前时间）

#### **早晨（6:00-9:00）：**

```
- "早上刷了XX分钟了，要不要先吃早饭？"
- "早上刷视频，会不会耽误今天的计划？"
```

#### **上午（9:00-12:00）：**

```
- "上午刷了XX分钟了，今天的工作/学习安排好了吗？"
- "上午刷视频，会不会影响今天的节奏？"
```

#### **午休（12:00-14:00）：**

```
- "午休时间刷刷视频挺好的，休息够了吗？"
- "午休刷了XX分钟，要不要眯一会儿？"
```

#### **下午（14:00-18:00）：**

```
- "下午刷了XX分钟，今天的任务完成了吗？"
- "下午刷视频，会不会耽误今天的事？"
```

#### **晚上（18:00-22:00）：**

```
- "晚上刷了XX分钟，今天过得怎么样？"
- "晚上刷视频，是在放松吗？"
```

#### **深夜（22:00-23:00）：**

```
- "都这么晚了，还不睡吗？明天还有事吧？"
- "深夜刷了XX分钟，明天会不会困？"
```

#### **凌晨（23:00-6:00）：**

```
不提醒（避免打扰睡眠）
```

---

### 3.5 情境感知文案（结合用户状态）

#### **工作日 vs 周末：**

```
工作日：
- "工作日刷了XX分钟，会不会影响工作？"
- "今天有工作要做吗？"

周末：
- "周末放松一下挺好的，今天有什么计划吗？"
- "周末刷视频，开心就好"
```

#### **连续多日刷视频：**

```
- "这是连续第X天刷视频了，最近还好吗？"
- "这周每天都在刷视频，是不是有什么压力？"
```

#### **本周第一次刷视频：**

```
- "这周还没怎么刷过视频呢，今天想放松一下？"
- "难得刷一次，开心就好"
```

---

## 四、交互设计

### 4.1 提醒样式设计

#### **视觉层级：轻 → 重**

```
15分钟（轻提醒）：
├─ 样式：顶部浮层，半透明
├─ 位置：屏幕上方 20% 处
├─ 大小：小（占屏幕宽度 80%）
├─ 动画：轻轻滑入
└─ 自动消失：5秒后

30分钟（中提醒）：
├─ 样式：居中卡片，微模糊背景
├─ 位置：屏幕中央
├─ 大小：中（占屏幕宽度 85%）
├─ 动画：缩放渐入
└─ 需手动操作：点击"知道了"或"继续看"

1小时（强提醒）：
├─ 样式：居中卡片，模糊背景
├─ 位置：屏幕中央
├─ 大小：大（占屏幕宽度 90%）
├─ 动画：缩放渐入 + 轻微抖动
└─ 需手动操作：必须选择一个选项

2小时+（重提醒）：
├─ 样式：全屏卡片，深色背景
├─ 位置：全屏居中
├─ 大小：占屏幕宽度 95%
├─ 动画：淡入 + 震动反馈
└─ 需手动操作：必须选择一个选项 + 确认
```

---

### 4.2 界面示意图（文字版）

#### **15分钟轻提醒：**

```
┌─────────────────────────────────────┐
│  ╭─────────────────────────────╮   │
│  │ 🕐 刷了15分钟了                │   │
│  │ 你刚刚想做什么来着？           │   │
│  │                                │   │
│  │  [还记得] [忘了] [再看一会儿]  │   │
│  ╰─────────────────────────────╯   │
│                                     │
│              (5秒后自动消失)         │
└─────────────────────────────────────┘
```

---

#### **30分钟中提醒：**

```
┌─────────────────────────────────────┐
│                                     │
│    ╭───────────────────────────╮   │
│    │  🕑 刷了半小时了            │   │
│    │  学到什么了吗？            │   │
│    │                            │   │
│    │  今天想学：____________    │   │
│    │  (选填)                    │   │
│    │                            │   │
│    │  [学到了] [没什么印象]     │   │
│    │  [起来休息] [再看一会儿]   │   │
│    ╰───────────────────────────╯   │
│                                     │
└─────────────────────────────────────┘
```

---

#### **1小时强提醒：**

```
┌─────────────────────────────────────┐
│                                     │
│     ╭─────────────────────────╮    │
│     │  🕐 刷了1小时了           │    │
│     │  起来倒杯水吧？          │    │
│     │                          │    │
│     │  今天已经刷了：          │    │
│     │  · 抖音 1小时            │    │
│     │  · 小红书 30分钟         │    │
│     │                          │    │
│     │  ┌──────────────────┐   │    │
│     │  │ 去倒水 🚰         │   │    │
│     │  └──────────────────┘   │    │
│     │  [走走] [继续看] [记录] │    │
│     ╰─────────────────────────╯    │
│                                     │
└─────────────────────────────────────┘
```

---

#### **2小时重提醒：**

```
┌─────────────────────────────────────┐
│                                     │
│      ╭────────────────────────╮    │
│      │  🕑 刷了2小时了         │    │
│      │  累不累？认真问的。    │    │
│      │                        │    │
│      │  今天刷视频：          │    │
│      │  · 累了想放松：1.5小时 │    │
│      │  · 逃避工作：30分钟    │    │
│      │                        │    │
│      │  你在逃避什么呢？      │    │
│      │  想聊聊吗？            │    │
│      │                        │    │
│      │  ┌─────────────────┐  │    │
│      │  │ 确实累了 😮‍💨      │  │    │
│      │  └─────────────────┘  │    │
│      │  [出去走走] [想聊聊]   │    │
│      │  [记录心情] [继续看]   │    │
│      ╰────────────────────────╯    │
│                                     │
└─────────────────────────────────────┘
```

---

### 4.3 用户操作流程

#### **流程1：轻提醒（15分钟）**

```
用户刷视频达到15分钟
         ↓
   顶部弹出轻提醒
         ↓
用户可以：
├─ 点击"还记得" → 记录原本要做什么 → 提示"要不要先去做？"
├─ 点击"忘了" → 引导记录动机 → 继续刷
├─ 点击"再看一会儿" → 关闭提醒 → 继续刷
└─ 不操作（5秒后自动消失） → 继续刷
```

---

#### **流程2：中提醒（30分钟）**

```
用户刷视频达到30分钟
         ↓
   居中弹出中提醒
         ↓
用户必须操作：
├─ 点击"学到了" → 引导记录笔记 → 关闭提醒
├─ 点击"没什么印象" → 提示"那要不要休息一下？" → 用户选择
├─ 点击"起来休息" → 记录休息行为 → 启动休息计时器
└─ 点击"再看一会儿" → 关闭提醒 → 下次提醒推迟到1小时
```

---

#### **流程3：强提醒（1小时）**

```
用户刷视频达到1小时
         ↓
   居中弹出强提醒（带震动）
         ↓
显示今天的统计数据
         ↓
用户必须选择一个行动：
├─ 点击"去倒水" → 记录行动 → 启动5分钟倒水计时器 → APP暂停
├─ 点击"走走" → 记录行动 → 启动10分钟散步计时器 → APP暂停
├─ 点击"记录" → 跳转到记录页面 → 引导记录今天的状态
└─ 点击"继续看" → 提示"下次2小时再提醒" → 关闭
```

---

#### **流程4：重提醒（2小时）**

```
用户刷视频达到2小时
         ↓
   全屏弹出重提醒（带强震动）
         ↓
显示今天的动机分析
         ↓
用户必须选择：
├─ 点击"确实累了" → 引导休息 → 提供放松建议
├─ 点击"出去走走" → 启动计时器 → APP暂停
├─ 点击"想聊聊" → 跳转到"心情记录"页面
├─ 点击"记录心情" → 跳转到记录页面
└─ 点击"继续看" → 二次确认："确定要继续吗？" → 用户确认后关闭
```

---

### 4.4 提醒关闭策略

#### **用户可以选择：**

```
1. 暂时关闭（本次刷视频不再提醒）
   └─ 但下次刷视频时重新开始计时

2. 今天关闭（今天不再提醒）
   └─ 但明天恢复提醒

3. 永久关闭（完全关闭提醒功能）
   └─ 需要二次确认
   └─ 提示："关闭后就没人陪伴你了，确定吗？"
```

---

## 五、技术实现

### 5.1 时长统计技术方案

#### **方案A：前台计时（精准但不隐私）**

```
需要用户授权：
├─ iOS：屏幕使用时间API（Screen Time API）
├─ Android：使用统计API（UsageStatsManager）
└─ 缺点：需要敏感权限，可能被拒绝

优点：
├─ 自动统计，无需手动记录
├─ 精准到秒
└─ 可以识别具体APP

缺点：
├─ 需要敏感权限
├─ 用户可能担心隐私
└─ 无法识别用户动机
```

**不推荐**（违背"隐私优先"原则）

---

#### **方案B：手动记录（隐私但依赖用户）** ⭐ 推荐

```
用户手动开始记录：
├─ 点击"开始刷抖音"
├─ 选择动机（学习/放松/逃避/无聊）
└─ APP开始计时

用户手动结束记录：
├─ 点击"结束记录"
└─ 或自动检测（APP进入后台15分钟后自动结束）

优点：
├─ 完全隐私（本地计时）
├─ 强制用户记录动机（增强觉察）
├─ 不需要敏感权限
└─ 符合产品理念

缺点：
├─ 依赖用户记录（但这本身就是"觉察"的一部分）
└─ 如果用户忘记记录，无法统计
```

**推荐**（符合产品理念）

---

#### **方案C：混合方案（精准且隐私）** ⭐⭐ 最推荐

```
1. 用户手动开始记录（选择动机）
2. APP在前台时自动计时
3. APP进入后台时：
   ├─ 如果5分钟内回到前台 → 继续计时
   ├─ 如果5-15分钟回到前台 → 询问"是否继续计时？"
   └─ 如果超过15分钟 → 自动结束记录
4. 用户可随时手动结束记录

优点：
├─ 兼顾隐私和便利
├─ 减少用户操作负担
├─ 仍然强制记录动机
└─ 符合产品理念

缺点：
├─ 技术实现略复杂
└─ 需要处理边界情况（如来电话、切换APP等）
```

**最推荐**（平衡性最好）

---

### 5.2 技术架构设计

```
┌──────────────────────────────────────────┐
│              前端UI层                     │
│  ┌────────────────────────────────────┐  │
│  │  提醒弹窗组件                       │  │
│  │  · 文案渲染                         │  │
│  │  · 动画效果                         │  │
│  │  · 用户交互                         │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────────┐
│            业务逻辑层                     │
│  ┌────────────────────────────────────┐  │
│  │  提醒触发引擎                       │  │
│  │  · 时长监听                         │  │
│  │  · 阈值判断                         │  │
│  │  · 场景识别（动机/时段/状态）      │  │
│  │  · 文案选择                         │  │
│  │  · 智能静默                         │  │
│  └────────────────────────────────────┘  │
│  ┌────────────────────────────────────┐  │
│  │  计时器管理                         │  │
│  │  · 前台计时                         │  │
│  │  · 后台检测                         │  │
│  │  · 自动结束                         │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
                    ↓
┌──────────────────────────────────────────┐
│             数据存储层                    │
│  ┌────────────────────────────────────┐  │
│  │  本地数据库（SQLite）               │  │
│  │  · 记录表（sessions）               │  │
│  │  · 提醒历史表（reminders）          │  │
│  │  · 用户设置表（settings）           │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

---

### 5.3 数据库设计

#### **表1：记录表（sessions）**

```sql
CREATE TABLE sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    platform VARCHAR(50) NOT NULL,        -- 平台（抖音/小红书等）
    start_time DATETIME NOT NULL,         -- 开始时间
    end_time DATETIME,                    -- 结束时间
    duration INTEGER,                     -- 时长（秒）
    motivation VARCHAR(50),               -- 动机（学习/放松/逃避/无聊）
    original_intention TEXT,              -- 原本要做什么（选填）
    note TEXT,                            -- 备注
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

#### **表2：提醒历史表（reminders）**

```sql
CREATE TABLE reminders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id INTEGER NOT NULL,          -- 关联的记录ID
    trigger_time DATETIME NOT NULL,       -- 触发时间
    duration_minutes INTEGER NOT NULL,    -- 触发时的时长（分钟）
    reminder_level VARCHAR(20) NOT NULL,  -- 提醒级别（light/medium/strong等）
    message TEXT NOT NULL,                -- 提醒文案
    user_action VARCHAR(50),              -- 用户操作（继续/休息/记录等）
    user_response TEXT,                   -- 用户回应（选填）
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id)
);
```

---

#### **表3：用户设置表（settings）**

```sql
CREATE TABLE settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    reminder_enabled BOOLEAN DEFAULT 1,   -- 是否开启提醒
    reminder_15min BOOLEAN DEFAULT 1,     -- 15分钟提醒
    reminder_30min BOOLEAN DEFAULT 1,     -- 30分钟提醒
    reminder_1hour BOOLEAN DEFAULT 1,     -- 1小时提醒
    reminder_2hour BOOLEAN DEFAULT 1,     -- 2小时提醒
    reminder_4hour BOOLEAN DEFAULT 1,     -- 4小时提醒
    night_mode BOOLEAN DEFAULT 1,         -- 夜间勿扰（23:00-07:00）
    vibration BOOLEAN DEFAULT 1,          -- 震动反馈
    last_updated DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 六、代码示例（React Native）

### 6.1 计时器核心逻辑

```javascript
// TimerManager.js
import { AppState } from 'react-native';
import AsyncStorage from '@react-native-async-storage/async-storage';

class TimerManager {
  constructor() {
    this.sessionId = null;
    this.startTime = null;
    this.elapsedSeconds = 0;
    this.intervalId = null;
    this.appStateSubscription = null;
    this.backgroundTime = null;
  }

  // 开始记录
  async startSession(platform, motivation, originalIntention = '') {
    this.sessionId = Date.now(); // 简单的ID生成
    this.startTime = new Date();
    this.elapsedSeconds = 0;
    
    // 保存到数据库
    await this.saveSessionStart(platform, motivation, originalIntention);
    
    // 开始计时
    this.startTimer();
    
    // 监听APP状态变化
    this.listenAppState();
    
    return this.sessionId;
  }

  // 开始计时器
  startTimer() {
    this.intervalId = setInterval(() => {
      this.elapsedSeconds++;
      
      // 每秒检查是否需要触发提醒
      this.checkReminder();
      
      // 保存当前进度（防止APP崩溃丢失数据）
      if (this.elapsedSeconds % 60 === 0) { // 每分钟保存一次
        this.saveProgress();
      }
    }, 1000);
  }

  // 检查是否需要触发提醒
  checkReminder() {
    const minutes = Math.floor(this.elapsedSeconds / 60);
    
    const thresholds = [15, 30, 60, 120, 240]; // 15分钟、30分钟、1小时、2小时、4小时
    
    if (thresholds.includes(minutes)) {
      // 触发提醒
      this.triggerReminder(minutes);
    }
    
    // 4小时后，每30分钟提醒一次
    if (minutes >= 240 && (minutes - 240) % 30 === 0) {
      this.triggerReminder(minutes);
    }
  }

  // 触发提醒
  async triggerReminder(minutes) {
    // 检查是否在静默期
    if (await this.shouldSilence()) {
      return;
    }
    
    // 获取提醒文案
    const message = await this.getReminderMessage(minutes);
    
    // 显示提醒弹窗
    this.showReminderModal(message, minutes);
    
    // 记录提醒历史
    await this.saveReminderHistory(minutes, message);
  }

  // 获取提醒文案
  async getReminderMessage(minutes) {
    const motivation = await this.getSessionMotivation();
    const timeOfDay = this.getTimeOfDay();
    const level = this.getReminderLevel(minutes);
    
    // 根据时长、动机、时段选择文案
    return ReminderMessages.get(level, motivation, timeOfDay);
  }

  // 判断提醒级别
  getReminderLevel(minutes) {
    if (minutes === 15) return 'light';
    if (minutes === 30) return 'medium';
    if (minutes === 60) return 'strong';
    if (minutes === 120) return 'heavy';
    if (minutes >= 240) return 'critical';
  }

  // 判断时段
  getTimeOfDay() {
    const hour = new Date().getHours();
    if (hour >= 6 && hour < 9) return 'morning';
    if (hour >= 9 && hour < 12) return 'forenoon';
    if (hour >= 12 && hour < 14) return 'noon';
    if (hour >= 14 && hour < 18) return 'afternoon';
    if (hour >= 18 && hour < 22) return 'evening';
    if (hour >= 22 || hour < 6) return 'night';
  }

  // 判断是否应该静默
  async shouldSilence() {
    // 检查夜间勿扰
    const hour = new Date().getHours();
    if (hour >= 23 || hour < 7) {
      return true; // 深夜不提醒
    }
    
    // 检查用户是否关闭了提醒
    const settings = await this.getSettings();
    if (!settings.reminder_enabled) {
      return true;
    }
    
    // 检查是否刚刚提醒过（5分钟内不重复提醒）
    const lastReminderTime = await this.getLastReminderTime();
    if (lastReminderTime && (Date.now() - lastReminderTime < 5 * 60 * 1000)) {
      return true;
    }
    
    return false;
  }

  // 监听APP状态变化
  listenAppState() {
    this.appStateSubscription = AppState.addEventListener('change', (nextAppState) => {
      if (nextAppState === 'background') {
        // APP进入后台
        this.onAppBackground();
      } else if (nextAppState === 'active') {
        // APP回到前台
        this.onAppForeground();
      }
    });
  }

  // APP进入后台
  onAppBackground() {
    this.backgroundTime = Date.now();
    // 暂停计时器（可选：也可以继续计时）
    this.pauseTimer();
  }

  // APP回到前台
  async onAppForeground() {
    if (!this.backgroundTime) return;
    
    const backgroundDuration = Math.floor((Date.now() - this.backgroundTime) / 1000);
    
    if (backgroundDuration < 5 * 60) {
      // 5分钟内回来，继续计时
      this.resumeTimer();
    } else if (backgroundDuration < 15 * 60) {
      // 5-15分钟，询问是否继续
      const shouldContinue = await this.askContinue(backgroundDuration);
      if (shouldContinue) {
        this.resumeTimer();
      } else {
        this.endSession();
      }
    } else {
      // 超过15分钟，自动结束
      this.endSession();
    }
    
    this.backgroundTime = null;
  }

  // 暂停计时器
  pauseTimer() {
    if (this.intervalId) {
      clearInterval(this.intervalId);
      this.intervalId = null;
    }
  }

  // 恢复计时器
  resumeTimer() {
    if (!this.intervalId) {
      this.startTimer();
    }
  }

  // 结束记录
  async endSession() {
    this.pauseTimer();
    
    // 保存最终数据
    await this.saveSessionEnd();
    
    // 清理
    if (this.appStateSubscription) {
      this.appStateSubscription.remove();
    }
    
    this.sessionId = null;
    this.startTime = null;
    this.elapsedSeconds = 0;
  }

  // ... 其他辅助方法（数据库操作等）
}

export default new TimerManager();
```

---

### 6.2 提醒弹窗组件

```javascript
// ReminderModal.js
import React, { useState, useEffect } from 'react';
import {
  Modal,
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
  Animated,
  Vibration,
} from 'react-native';

const ReminderModal = ({ visible, level, message, duration, onAction }) => {
  const [fadeAnim] = useState(new Animated.Value(0));
  const [scaleAnim] = useState(new Animated.Value(0.8));

  useEffect(() => {
    if (visible) {
      // 震动反馈
      if (level === 'strong' || level === 'heavy' || level === 'critical') {
        Vibration.vibrate([0, 100, 50, 100]); // 震动模式
      }

      // 动画
      Animated.parallel([
        Animated.timing(fadeAnim, {
          toValue: 1,
          duration: 300,
          useNativeDriver: true,
        }),
        Animated.spring(scaleAnim, {
          toValue: 1,
          friction: 5,
          useNativeDriver: true,
        }),
      ]).start();
    }
  }, [visible]);

  const handleAction = (action) => {
    // 关闭动画
    Animated.parallel([
      Animated.timing(fadeAnim, {
        toValue: 0,
        duration: 200,
        useNativeDriver: true,
      }),
      Animated.timing(scaleAnim, {
        toValue: 0.8,
        duration: 200,
        useNativeDriver: true,
      }),
    ]).start(() => {
      onAction(action);
    });
  };

  // 根据级别选择样式
  const getContainerStyle = () => {
    const baseStyle = styles.container;
    if (level === 'light') return [baseStyle, styles.lightContainer];
    if (level === 'medium') return [baseStyle, styles.mediumContainer];
    if (level === 'strong') return [baseStyle, styles.strongContainer];
    if (level === 'heavy' || level === 'critical') return [baseStyle, styles.heavyContainer];
    return baseStyle;
  };

  // 根据级别选择操作按钮
  const renderActions = () => {
    if (level === 'light') {
      return (
        <View style={styles.actions}>
          <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('remember')}>
            <Text style={styles.actionText}>还记得</Text>
          </TouchableOpacity>
          <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('forgot')}>
            <Text style={styles.actionText}>忘了</Text>
          </TouchableOpacity>
          <TouchableOpacity style={[styles.actionButton, styles.primaryAction]} onPress={() => handleAction('continue')}>
            <Text style={styles.primaryActionText}>再看一会儿</Text>
          </TouchableOpacity>
        </View>
      );
    }

    if (level === 'medium') {
      return (
        <View style={styles.actions}>
          <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('learned')}>
            <Text style={styles.actionText}>学到了</Text>
          </TouchableOpacity>
          <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('nothing')}>
            <Text style={styles.actionText}>没什么印象</Text>
          </TouchableOpacity>
          <TouchableOpacity style={[styles.actionButton, styles.primaryAction]} onPress={() => handleAction('rest')}>
            <Text style={styles.primaryActionText}>起来休息</Text>
          </TouchableOpacity>
          <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('continue')}>
            <Text style={styles.actionText}>再看一会儿</Text>
          </TouchableOpacity>
        </View>
      );
    }

    if (level === 'strong') {
      return (
        <>
          <TouchableOpacity
            style={[styles.actionButton, styles.primaryActionLarge]}
            onPress={() => handleAction('water')}
          >
            <Text style={styles.primaryActionLargeText}>去倒水 🚰</Text>
          </TouchableOpacity>
          <View style={styles.actions}>
            <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('walk')}>
              <Text style={styles.actionText}>走走</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('record')}>
              <Text style={styles.actionText}>记录</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('continue')}>
              <Text style={styles.actionText}>继续看</Text>
            </TouchableOpacity>
          </View>
        </>
      );
    }

    if (level === 'heavy' || level === 'critical') {
      return (
        <>
          <TouchableOpacity
            style={[styles.actionButton, styles.primaryActionLarge]}
            onPress={() => handleAction('tired')}
          >
            <Text style={styles.primaryActionLargeText}>确实累了 😮‍💨</Text>
          </TouchableOpacity>
          <View style={styles.actions}>
            <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('walk')}>
              <Text style={styles.actionText}>出去走走</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('talk')}>
              <Text style={styles.actionText}>想聊聊</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('mood')}>
              <Text style={styles.actionText}>记录心情</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.actionButton} onPress={() => handleAction('continue')}>
              <Text style={styles.actionText}>继续看</Text>
            </TouchableOpacity>
          </View>
        </>
      );
    }
  };

  return (
    <Modal transparent visible={visible} animationType="none">
      <View style={styles.overlay}>
        <Animated.View
          style={[
            getContainerStyle(),
            {
              opacity: fadeAnim,
              transform: [{ scale: scaleAnim }],
            },
          ]}
        >
          {/* 时长显示 */}
          <View style={styles.header}>
            <Text style={styles.timeIcon}>🕐</Text>
            <Text style={styles.durationText}>刷了{duration}了</Text>
          </View>

          {/* 提醒文案 */}
          <Text style={styles.messageText}>{message}</Text>

          {/* 操作按钮 */}
          {renderActions()}
        </Animated.View>
      </View>
    </Modal>
  );
};

const styles = StyleSheet.create({
  overlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
    justifyContent: 'center',
    alignItems: 'center',
  },
  container: {
    width: '85%',
    backgroundColor: '#fff',
    borderRadius: 20,
    padding: 24,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 8,
  },
  lightContainer: {
    width: '80%',
    padding: 20,
  },
  mediumContainer: {
    width: '85%',
  },
  strongContainer: {
    width: '90%',
  },
  heavyContainer: {
    width: '95%',
    backgroundColor: '#f9f9f9',
  },
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 12,
  },
  timeIcon: {
    fontSize: 24,
    marginRight: 8,
  },
  durationText: {
    fontSize: 18,
    fontWeight: '600',
    color: '#333',
  },
  messageText: {
    fontSize: 16,
    color: '#666',
    lineHeight: 24,
    marginBottom: 20,
  },
  actions: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
    marginTop: 12,
  },
  actionButton: {
    paddingVertical: 12,
    paddingHorizontal: 20,
    borderRadius: 12,
    backgroundColor: '#f5f5f5',
    marginBottom: 8,
    minWidth: '30%',
    alignItems: 'center',
  },
  primaryAction: {
    backgroundColor: '#007AFF',
  },
  primaryActionLarge: {
    width: '100%',
    paddingVertical: 16,
    backgroundColor: '#007AFF',
    marginBottom: 12,
  },
  actionText: {
    fontSize: 14,
    color: '#333',
    fontWeight: '500',
  },
  primaryActionText: {
    fontSize: 14,
    color: '#fff',
    fontWeight: '600',
  },
  primaryActionLargeText: {
    fontSize: 16,
    color: '#fff',
    fontWeight: '600',
  },
});

export default ReminderModal;
```

---

### 6.3 文案管理

```javascript
// ReminderMessages.js

const messages = {
  light: {
    default: [
      "刷了15分钟了，你刚刚想做什么来着？",
      "15分钟过去了，找到想看的了吗？",
      "刷了一会儿了，还记得原本要做什么吗？",
      "15分钟了，要不要先做正事？",
      "刷了15分钟，你是来学习的吗？还是放松一下？",
    ],
    learning: [
      "学了15分钟了，要不要记个笔记？",
      "15分钟了，学到什么了？",
    ],
    relaxing: [
      "放松15分钟了，感觉好点了吗？",
      "15分钟了，心情好点了吗？",
    ],
    escaping: [
      "在逃避什么呢？要不要先面对一下？",
      "15分钟了，逃避能解决问题吗？",
    ],
    boring: [
      "无聊了15分钟，要不要换个方式？",
      "15分钟了，还无聊吗？",
    ],
  },
  medium: {
    default: [
      "刷了半小时了，学到什么了吗？",
      "30分钟过去了，记得什么内容吗？",
      "半小时了，累不累？要不要休息一下？",
      "刷了30分钟，你在找什么呢？",
      "半小时了，是在放松吗？感觉怎么样？",
      "30分钟过去了，眼睛累不累？",
    ],
    learning: [
      "学了半小时，记得什么了吗？",
      "30分钟了，要不要整理一下笔记？",
    ],
    relaxing: [
      "放松半小时了，够了吗？",
      "30分钟了，感觉轻松些了吗？",
    ],
    escaping: [
      "逃避半小时了，那件事还在等你呢",
      "30分钟了，要不要试着做一下？",
    ],
    boring: [
      "无聊半小时了，要不要出去走走？",
      "30分钟了，有没有想做的事？",
    ],
  },
  strong: {
    default: [
      "刷了1小时了，起来倒杯水吧？",
      "1小时过去了，站起来走走？看看窗外？",
      "刷了1小时，眼睛休息一下吧？",
      "1小时了，伸个懒腰？动一动？",
      "刷了1小时了，要不要先去做那件事？",
      "1小时过去了，你在逃避什么吗？",
      "刷了1小时，今天还有其他想做的事吗？",
    ],
    learning: [
      "学了1小时了，休息一下，劳逸结合？",
      "1小时过去了，今天的学习目标完成了吗？",
    ],
    relaxing: [
      "放松1小时了，要不要换个方式？出去走走？",
      "1小时了，放松得差不多了吧？",
    ],
    escaping: [
      "逃避1小时了，其实可能没那么难？",
      "1小时了，要不要先做5分钟试试？",
    ],
    boring: [
      "无聊1小时了，要不要找点有意思的事做？",
      "1小时了，真的没有想做的事吗？",
    ],
  },
  heavy: {
    default: [
      "刷了2小时了，累不累？认真问的。",
      "2小时过去了，今天记得什么了吗？",
      "刷了2小时，是不是有什么心事？",
      "2小时了，要不要出去透透气？",
      "刷了2小时，眼睛该休息了吧？",
      "2小时过去了，你在逃避什么呢？想聊聊吗？",
      "刷了2小时，今天还有什么想做的事吗？",
    ],
    learning: [
      "学了2小时了，该休息了，真的",
      "2小时过去了，今天学得怎么样？",
    ],
    relaxing: [
      "放松2小时了，真的放松了吗？",
      "2小时了，会不会更累了？",
    ],
    escaping: [
      "逃避2小时了，还要继续吗？",
      "2小时了，不如先面对一下？",
    ],
    boring: [
      "无聊2小时了，是不是该做点什么了？",
      "2小时了，这样无聊下去不是办法吧？",
    ],
  },
  critical: {
    default: [
      "刷了4小时了，真的累了吧？起来动一动？",
      "4小时过去了，眼睛、脖子、腰都该休息了",
      "刷了4小时，今天过得怎么样？想聊聊吗？",
      "4小时了，是不是有什么不想面对的事？",
      "刷了4小时，要不要记录一下今天的心情？",
      "4小时过去了，出去走走吧，认真的",
      "刷了4小时，今天还好吗？需要帮助吗？",
    ],
  },
};

class ReminderMessages {
  static get(level, motivation = 'default', timeOfDay = null) {
    // 获取该级别的文案池
    const levelMessages = messages[level] || messages.strong;
    
    // 优先使用动机相关文案
    const motivationMessages = levelMessages[motivation] || levelMessages.default;
    
    // 随机选择一条
    const randomIndex = Math.floor(Math.random() * motivationMessages.length);
    let message = motivationMessages[randomIndex];
    
    // 时段优化（可选）
    if (timeOfDay === 'morning') {
      message = message.replace('累不累', '困不困');
    }
    
    return message;
  }
}

export default ReminderMessages;
```

---

## 七、A/B测试方案

### 7.1 测试假设

**假设1：疑问句式 vs 陈述句式**

```
A组（疑问句）："刷了1小时了，累不累？"
B组（陈述句）："你已经刷了1小时了，该休息了"

测试指标：
├─ 用户采纳率（点击"休息"的比例）
├─ 用户反感率（关闭提醒的比例）
└─ 后续行为（是否真的休息）
```

---

**假设2：具体行动 vs 空洞建议**

```
A组（具体行动）："起来倒杯水吧？"
B组（空洞建议）："休息一下吧"

测试指标：
├─ 用户采纳率
├─ 行动完成率（是否真的去倒水）
└─ 用户满意度
```

---

**假设3：提醒频率（激进 vs 保守）**

```
A组（激进）：15分钟、30分钟、1小时、2小时、4小时
B组（保守）：30分钟、1小时、2小时

测试指标：
├─ 用户留存率
├─ 提醒关闭率
└─ 用户反馈
```

---

### 7.2 测试流程

```
第1周：50% A组，50% B组
第2周：分析数据，确定优胜组
第3周：100%采用优胜组方案
第4周：持续优化
```

---

## 八、上线后优化策略

### 8.1 数据监控指标

```
核心指标：
├─ 提醒触发率（每个级别的触发次数）
├─ 用户采纳率（点击"休息"等积极行动的比例）
├─ 用户反感率（关闭提醒的比例）
├─ 行动完成率（是否真的去做了）
└─ 用户满意度（NPS评分）

次要指标：
├─ 提醒后的刷视频时长变化
├─ 用户记录动机的频率
├─ 用户查看晚报的频率
└─ 用户推荐产品的比例
```

---

### 8.2 优化方向

```
如果：用户采纳率低
→ 优化文案（更温柔、更具体）

如果：用户反感率高
→ 降低提醒频率，或增加自定义选项

如果：行动完成率低
→ 给出更具体的引导，或增加行动追踪

如果：用户满意度低
→ 收集用户反馈，重新设计
```

---

## 九、常见问题FAQ

### Q1：用户会不会觉得提醒很烦？

**A：关键在于文案和频率**

- 温柔疑问句 > 教育式命令
- 每个阶段只提醒1次 > 反复弹窗
- 用户可以关闭 > 强制弹窗
- 具体行动引导 > 空洞说教

---

### Q2：如果用户关闭提醒怎么办？

**A：尊重用户选择，但提供恢复入口**

- 用户可以随时在设置中重新开启
- 关闭提醒时，温柔询问："确定不需要陪伴了吗？"
- 即使关闭提醒，晚报仍然提供觉察功能

---

### Q3：如何避免误触发（如用户正在专注看教程）？

**A：智能场景识别**

- 如果用户记录动机是"学习"，提醒更温柔
- 如果用户暂停视频、切换视频时才弹窗（不在观看中途打断）
- 用户可以设置"专注模式"（暂时关闭提醒）

---

### Q4：提醒会不会消耗电量？

**A：优化计时器逻辑**

- 使用JavaScript的`setInterval`（轻量级）
- APP进入后台后暂停计时（不占用资源）
- 只在前台活跃时计时

---

## 十、总结

### 核心设计理念

```
1. 温柔陪伴 > 严格监督
2. 疑问引导 > 命令强制
3. 具体行动 > 空洞建议
4. 尊重选择 > 强制干预
5. 觉察为先 > 改变为辅
```

### 关键成功要素

```
1. 精准的时长统计
2. 温柔的文案设计
3. 适当的触发时机
4. 简洁的交互设计
5. 持续的优化迭代
```

### 预期效果

```
用户反馈：
"这个提醒不烦，像朋友在关心我"
"看到'起来倒杯水吧'我真的去了"
"刷了2小时被提醒，才发现时间过得这么快"

数据效果：
├─ 用户采纳率 > 60%
├─ 用户反感率 < 10%
├─ 用户留存率 > 80%
└─ 用户推荐率 > 50%
```

---

**功能设计完成！准备开发！** 🚀
